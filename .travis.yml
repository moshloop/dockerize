language: go

go:
  - "1.9"

before_install: go get github.com/mitchellh/gox
script:  make
deploy:
  provider: releases
  api_key: $GITHUB_TOKEN
  file:
    - build/waiter
    - build/waiter.exe
    - build/waiter_osx
  skip_cleanup: true
  on:
    tags: true